PR Draft: N+1 Audit & Rate Limiting Sprint

Summary
-------
Small-to-medium risk refactor focused on eliminating N+1 query hotspots across reporting and analytics endpoints and consolidating a few executive aggregates into single queries. Also added safer caching inside `ScenarioAnalyticsService` to avoid repeated per-item DB calls.

Files changed (key)
-------------------
- app/Services/ScenarioAnalyticsService.php
- app/Services/ImpactReportService.php
- app/Services/TalentRoiService.php
- app/Services/Intelligence/ImpactEngineService.php
- openmemory.md

What changed
------------
- Prefetching: `ScenarioAnalyticsService::ensureScenarioCache()` now precaches `competency_names` and several aggregates (competency_skills, scenario_role_skills, people_role_skills aggregates, closure strategies, etc.).
- `calculateImpact()` now uses the scenario cache exclusively and avoids per-skill/role fallback DB queries.
- `ImpactReportService` passes loaded `Scenario` models through to avoid duplicate finds, and `getTalentPipelineMetrics()` consolidates three counts into a single SQL.
- `TalentRoiService::getExecutiveAggregates()` was extended to include `avg_turnover_risk` and other aggregates; `getExecutiveSummary()` reuses these aggregates and exposes some for callers to avoid duplicate counts.
- Several call sites (ImpactReportService) now reuse precomputed aggregates (e.g., `upskilled_count`) instead of re-querying.

Measured impact (full N+1 scan)
-------------------------------
Artifacts: `storage/logs/nplusone_full_report.csv` and `.json` (generated by `tests/Feature/NPlusOneFullScanTest.php`)

Example before → after:
- `/api/reports/consolidated`: ~27 → 14 queries
- `/api/reports/roi`: ~26 → 13 queries
- `/api/investor/dashboard`: ~13 → 9 queries
- `/api/departments/heatmap`: ~51 → 2 queries
- `/api/dashboard/metrics`: 88 → 7 queries (earlier work)

Repro steps
-----------
From repo root:

```bash
# run only the N+1 scan test (fast)
php artisan test tests/Feature/NPlusOneFullScanTest.php --filter=scan_all_get_api_routes_and_report -v

# view CSV report
cat storage/logs/nplusone_full_report.csv

# view JSON report
jq . storage/logs/nplusone_full_report.json | less
```

Notes / Next steps
------------------
- Remaining work: investigate `business_metrics` + `financial_indicators` use patterns to see if more batching is beneficial; optionally aggregate `pulse_responses`/`employee_pulses` into executive aggregates for reuse.
- Suggest code review on the extended SQL in `TalentRoiService::getExecutiveAggregates()` (large subselect) for DB portability and explainability.

Testing & Formatting
--------------------
- Ran the N+1 scan test; all tests pass locally.
- Please run `vendor/bin/pint --dirty` before finalizing PR if formatting is required.

Files to review first
---------------------
1. app/Services/ScenarioAnalyticsService.php — cache shape and `calculateImpact()` behaviour.
2. app/Services/TalentRoiService.php — `getExecutiveAggregates()` SQL and usage.
3. app/Services/ImpactReportService.php — call sites now reuse aggregates.

---

If you want, I can also:
- Open a branch + create a PR template (requires git push access).
- Run a broader test subset or CI commands.
- Continue iterating to reduce `/api/reports/consolidated` further by combining `business_metrics` + `financial_indicators` reads.
