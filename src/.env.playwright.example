# Example environment variables for Playwright E2E tests
# Copy to `.env.playwright` (or set in CI) and keep secrets out of source control.
E2E_ADMIN_EMAIL=admin@example.com
E2E_ADMIN_PASSWORD=password
